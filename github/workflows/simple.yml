name: Shell Commands

on: [push]

jobs: 
  run-shell-command:
    runs-on: ubunto-latest
    steps:
      - name: echo a string
        run: echo "Hello"
      - name: multiline script
        run: 
          node -v
          npm -v